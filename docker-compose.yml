version: "3.8"

services:
  chat:
    build: .
    ports:
      - "8766:8766"
    volumes:
      - ./messages:/app/messages
      - ./agents.json:/app/agents.json:ro
    environment:
      - AGENTCHAT_HOST=0.0.0.0
      - AGENTCHAT_PORT=8766
